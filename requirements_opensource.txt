# Open-Source Version (No API keys required)
# Total cost: $0

# Core dependencies
numpy>=1.24.0
scikit-learn>=1.3.0

# Local embeddings (replaces OpenAI API)
sentence-transformers>=2.2.0
torch>=2.0.0  # Required by sentence-transformers

# Note: Ollama must be installed separately
# Install: curl -fsSL https://ollama.com/install.sh | sh
# Then: ollama pull qwen2.5:7b

# ============================================
# Optional: If you have API keys, you can use the premium version
# Uncomment these for cloud-based models:
# anthropic>=0.42.0
# openai>=1.52.0
